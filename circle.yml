machine:
  environment:
    PATH: "${PATH}:${HOME}/.cargo/bin"
dependencies:
  cache_directories:
    - "~/.cargo"
  override:
    - "[ -f ~/.cargo/bin/cargo ] || curl https://sh.rustup.rs -sSf | sh -s -- -y"
test:
  override:
    - cargo test
